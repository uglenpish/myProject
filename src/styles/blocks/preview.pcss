.preview{
	display: flex;
	flex-direction: column;
	align-items: center;

	.preview__desc{
		margin-bottom: 35px;
		text-align: center;

		.preview__desc-text {
			font-size: 18px;
			line-height: 30px;
			font-weight: 600;
		}
	}

	.preview__username{
		font-size: 72px;
		line-height: 60px;
		font-weight: 800;
	}
}